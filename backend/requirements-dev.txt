# =============================================================================
# RythmoAI Backend - Dépendances Développement & Test (§18.4 CDC)
# =============================================================================
# Outils de développement, test, lint et documentation
# Installation complémentaire aux requirements.txt
#
# Utilisation:
#   pip install -r requirements.txt
#   pip install -r requirements-dev.txt
#
# Ce profil installe:
# - Tests: pytest, pytest-asyncio, httpx
# - Code quality: ruff, mypy, black (optionnel)
# - Sécurité: pip-audit, safety
# - Documentation: sphinx (optionnel)
# =============================================================================

# -----------------------------------------------------------------------------
# Tests (§18.4)
# -----------------------------------------------------------------------------
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-timeout>=2.2.0
httpx>=0.26.0

# -----------------------------------------------------------------------------
# Test client pour API asynchrone
# -----------------------------------------------------------------------------
httpx2>=0.2.0  # Pour starlette.testclient (FastAPI)

# -----------------------------------------------------------------------------
# Tests RLS PostgreSQL (§9.6) — isolation multi-tenant au niveau moteur
# -----------------------------------------------------------------------------
# pgserver : PostgreSQL 16 embarqué pour les tests d'intégration RLS sans
#            service externe ; psycopg2-binary : client synchrone pour les
#            connexions multi-rôles du test d'isolation.
pgserver>=0.1.3
psycopg2-binary>=2.9.0

# -----------------------------------------------------------------------------
# Code Quality & Linting
# -----------------------------------------------------------------------------
ruff>=0.1.0
mypy>=1.8.0
types-requests>=2.31.0
types-python-dateutil>=2.8.0
types-PyYAML>=6.0.0

# -----------------------------------------------------------------------------
# Formatage (optionnel - ruff format peut remplacer black)
# -----------------------------------------------------------------------------
# black>=23.0.0  # Désactivé, ruff format utilisé à la place

# -----------------------------------------------------------------------------
# Sécurité & Dépendances
# -----------------------------------------------------------------------------
pip-audit>=2.6.0
safety>=3.0.0

# -----------------------------------------------------------------------------
# Documentation (optionnel)
# -----------------------------------------------------------------------------
# sphinx>=7.0.0
# sphinx-rtd-theme>=1.3.0

# -----------------------------------------------------------------------------
# Outils de développement utiles
# -----------------------------------------------------------------------------
pre-commit>=3.5.0
rich>=13.0.0  # Pour les logs colorés en dev

# -----------------------------------------------------------------------------
# Variables d'environnement pour le développement
# -----------------------------------------------------------------------------
# .env.example est fourni dans backend/
# Copier en .env et adapter:
#   cp .env.example .env
